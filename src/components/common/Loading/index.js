import React from "react";
import { Container, Spinner } from './style'

const Loading = () => {
    return (
        <Container>
            <Spinner />
        </Container>
    );
}

export default Loading;